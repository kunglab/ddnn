#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='hybrid',
      version='1',
      description='Embedded Learning as a Service',
      author='',
      author_email='',
      packages=find_packages(),
     )
